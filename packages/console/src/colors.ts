/**
 * The console palette in JavaScript, for the canvas charts (which cannot read CSS custom
 * properties). Kept in step with the same tokens in styles.css. Ink instrument base, a
 * periwinkle engagement accent, and a reserved amber for the scrape-attempt signal.
 */
export const C = {
  accent: "#86a8ff",
  accentSoft: "rgba(134,168,255,0.18)",
  alert: "#f4a94b",
  pos: "#4fd39a",
  grid: "rgba(255,255,255,0.06)",
  axis: "#6b7486",
  surface: "#12151c",
} as const;
